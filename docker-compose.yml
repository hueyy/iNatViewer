services:
  inatviewer-production:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8000}:80"
    volumes: 
      - ./app/public/images:/srv/public/images
    restart: unless-stopped
